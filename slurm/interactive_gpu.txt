salloc --partition=gpu --gres=gpu:1 --cpus-per-task=8 --mem=48G --time=02:00:00
conda activate cryobednet
python - <<'PY'
import torch
print(torch.cuda.is_available())
print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU')
PY
